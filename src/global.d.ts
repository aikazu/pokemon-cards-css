/**
 * Global type declarations for the Pokemon Cards CSS project.
 */

interface Window {
  /** Google Analytics gtag function, injected via index.html script tag */
  gtag: (...args: any[]) => void;
}
