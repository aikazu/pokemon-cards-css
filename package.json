{
  "name": "pokemon-deck-builder",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "format": "bunx prettier --write 'src/**/*.{js,svelte,json}'",
    "format:check": "bunx prettier --check 'src/**/*.{js,svelte,json}'"
  },
  "dependencies": {
    "pokemontcgsdk": "^2.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.1.0",
    "svelte": "^3.52.0",
    "vite": "^3.2.0"
  }
}
